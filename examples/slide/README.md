See [this SO question](https://stackoverflow.com/questions/64598595/merging-columns-in-sliding-window-steps) for context.

### awk version

    awk -f slide.awk data.tsv

### tawk version

    tawk -f slide.tawk data.tsv
